# basket, brands, checkout, loyalty succeed

# you can use this command for a simple service generation.
# change the api file name and service name instead of demo
# -r is optional if you want to rewrite existed files
./surfGen generate ~/swagger/bethowen-swagger/loyalty/api.yaml -c kotlin.config.yaml -n brands -r
